{
  "mcpServers": {
    "signoff": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/mcp.mjs",
        "--vault",
        "${CLAUDE_PROJECT_DIR}/.signoff"
      ]
    }
  }
}